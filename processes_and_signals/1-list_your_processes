#!/usr/bin/env bash
# This script lists all currently running processes for all users,
# including those without a TTY, in a user-oriented format,
# and shows the process hierarchy.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
